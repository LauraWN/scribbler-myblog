<h2>Comments</h2>

<ul>
  <% @commments.each do |comment| %>
    <li>
      <%= link_to "#{comment.commenttext} - #{comment.post.title}",post_comment_path(comment.post,comment) %>
    </li>
  <% end %>
</ul>
